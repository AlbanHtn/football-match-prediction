# Copy this file to .env and fill in the values
# .env is gitignored — never commit secrets

# Optional: override default data directory paths
# FOOTBALL_DATA_RAW_DIR=/absolute/path/to/raw/data
# FOOTBALL_DATA_PROCESSED_DIR=/absolute/path/to/processed/data
